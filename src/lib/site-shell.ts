/** Shared horizontal shell for navbar + footer alignment. */
export const SITE_SHELL_MAX = "max-w-[1440px]";

export const SITE_SHELL_PADDING = "px-4 sm:px-6 lg:px-8";

export const SITE_SHELL_CLASS = `mx-auto w-full ${SITE_SHELL_MAX} ${SITE_SHELL_PADDING}`;

/** Page body shell: same width/padding as the navbar. */
export const SITE_PAGE_SHELL_CLASS = SITE_SHELL_CLASS;
